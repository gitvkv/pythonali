# Defining Lists with Square Brackets
<span class="category-tag">🏷️ Lists and List Operations /  Creating and Accessing Lists</span>

=== "📚 All-in-One"
    In Python, a list is one of the most versatile and commonly used data structures. Think of it as a container that can hold multiple items in a specific order. Whether you're storing server names, IP addresses, configuration values, or log entries, lists give you a simple way to group related data together. The most straightforward way to create a list is by using square brackets **[]**.
    
    ---
    
    ### ⚙️ What is a List?
    
    - A list is an **ordered collection** of items, meaning each item has a position (index) starting from **0**.
    - Lists can hold **different data types** in the same list — strings, numbers, booleans, or even other lists.
    - Lists are **mutable**, which means you can change, add, or remove items after the list is created.
    - Items inside a list are separated by **commas**.
    
    ---
    
    ### 📊 Creating a List with Square Brackets
    
    - To define a list, simply place your items inside square brackets **[]**.
    - The syntax is: **variable_name = [item1, item2, item3]**
    
    **Example — An empty list:**  
    **empty_list = []**
    
    **Example — A list of server names (strings):**  
    **servers = ["web01", "db01", "cache01"]**
    
    **Example — A list of numbers (integers):**  
    **ports = [80, 443, 22, 3306]**
    
    **Example — A mixed data type list:**  
    **mixed = ["nginx", 1.5, True, 443]**
    
    ---
    
    ### 🛠️ Key Characteristics of Lists
    
    - **Ordered:** Items maintain the order in which you define them.
    - **Indexed:** Each item has a position. The first item is at index **0**, second at **1**, and so on.
    - **Flexible:** You can store any data type — strings, integers, floats, booleans, even other lists.
    - **Dynamic:** Lists can grow or shrink as needed. You are not limited to a fixed size.
    
    ---
    
    ### 🕵️ Accessing Items in a List
    
    - Use the list name followed by the index number inside square brackets to access a specific item.
    - Indexing starts at **0**.
    
    **Example — Accessing the first server:**  
    **servers = ["web01", "db01", "cache01"]**  
    **first_server = servers[0]** → This gives you **"web01"**
    
    **Example — Accessing the last item using negative indexing:**  
    **last_server = servers[-1]** → This gives you **"cache01"**
    
    **Example — Accessing the second item:**  
    **second_server = servers[1]** → This gives you **"db01"**
    
    ---
    
    ### 📋 Comparison: List vs Other Simple Data Types
    
    | Feature | List | String | Integer |
    |---------|------|--------|---------|
    | Holds multiple items | ✅ Yes | ❌ No (single text) | ❌ No (single number) |
    | Mutable (changeable) | ✅ Yes | ❌ No (immutable) | ❌ No (immutable) |
    | Ordered | ✅ Yes | ✅ Yes | N/A |
    | Indexed access | ✅ Yes | ✅ Yes | N/A |
    | Mixed data types | ✅ Yes | ❌ No | ❌ No |
    
    ---
    
    ### 🧪 Practical Examples for Engineers
    
    **Example 1 — Storing environment names:**  
    **environments = ["dev", "staging", "prod"]**  
    **current_env = environments[1]** → Result: **"staging"**
    
    **Example 2 — Storing IP addresses as strings:**  
    **ip_list = ["10.0.1.1", "10.0.1.2", "10.0.1.3"]**  
    **first_ip = ip_list[0]** → Result: **"10.0.1.1"**
    
    **Example 3 — Storing configuration flags:**  
    **config_flags = [True, False, True, False]**  
    **first_flag = config_flags[0]** → Result: **True**
    
    **Example 4 — A list of lists (nested list):**  
    **network_config = [["web", "10.0.1.1"], ["db", "10.0.1.2"]]**  
    **first_pair = network_config[0]** → Result: **["web", "10.0.1.1"]**  
    **first_service = network_config[0][0]** → Result: **"web"**
    
    ---
    
    ### ✅ Summary
    
    - Lists are defined using **square brackets []**.
    - Items are separated by **commas**.
    - Lists are **ordered**, **mutable**, and can hold **mixed data types**.
    - Access items using **index numbers** starting from **0**.
    - Negative indexing (e.g., **-1**) lets you access items from the end of the list.
    
    Lists are a foundational building block in Python. Once you master defining and accessing them with square brackets, you'll be ready to explore more powerful list operations like adding, removing, and iterating over items.
    
    !!! info "Interactive Views"
        You are currently in **📚 All-in-One** mode. Use the tabs at the top to switch to **📖 Theory Only** or **💻 Code Only** views.

    ---

    A list in Python is an ordered collection of items created by placing values inside square brackets `[]`, separated by commas.
    
    ---
    
    ### 🟢 Example 1: Creating an empty list
    
    This example shows how to define a list with no items inside it.
    
    ```python
    empty_list = []
    ```
    
    📤 Output: **[]**
    
    ---
    
    ### 🟢 Example 2: Creating a list of numbers
    
    This example shows a list containing three integer values.
    
    ```python
    numbers = [10, 20, 30]
    ```
    
    📤 Output: **[10, 20, 30]**
    
    ---
    
    ### 🟢 Example 3: Creating a list of strings
    
    This example shows a list containing text values.
    
    ```python
    cities = ["New York", "London", "Tokyo"]
    ```
    
    📤 Output: **['New York', 'London', 'Tokyo']**
    
    ---
    
    ### 🟢 Example 4: Creating a mixed-type list
    
    This example shows a list containing different data types together.
    
    ```python
    mixed = [42, "engineer", 3.14, True]
    ```
    
    📤 Output: **[42, 'engineer', 3.14, True]**
    
    ---
    
    ### 🟢 Example 5: Creating a list from variables
    
    This example shows how to use existing variables to build a list.
    
    ```python
    sensor_id = "TEMP-001"
    reading = 23.5
    unit = "Celsius"
    
    log_entry = [sensor_id, reading, unit]
    ```
    
    📤 Output: **['TEMP-001', 23.5, 'Celsius']**
    
    ---
    
    ### 📊 Quick Reference: Defining Lists
    
    | Syntax | Description | Example |
    |--------|-------------|---------|
    | `[]` | Empty list | `data = []` |
    | `[value1, value2, ...]` | List with items | `ports = [80, 443, 22]` |
    | `[var1, var2, ...]` | List from variables | `config = [host, port, timeout]` |

=== "📖 Theory Only"
    In Python, a list is one of the most versatile and commonly used data structures. Think of it as a container that can hold multiple items in a specific order. Whether you're storing server names, IP addresses, configuration values, or log entries, lists give you a simple way to group related data together. The most straightforward way to create a list is by using square brackets **[]**.
    
    ---
    
    ### ⚙️ What is a List?
    
    - A list is an **ordered collection** of items, meaning each item has a position (index) starting from **0**.
    - Lists can hold **different data types** in the same list — strings, numbers, booleans, or even other lists.
    - Lists are **mutable**, which means you can change, add, or remove items after the list is created.
    - Items inside a list are separated by **commas**.
    
    ---
    
    ### 📊 Creating a List with Square Brackets
    
    - To define a list, simply place your items inside square brackets **[]**.
    - The syntax is: **variable_name = [item1, item2, item3]**
    
    **Example — An empty list:**  
    **empty_list = []**
    
    **Example — A list of server names (strings):**  
    **servers = ["web01", "db01", "cache01"]**
    
    **Example — A list of numbers (integers):**  
    **ports = [80, 443, 22, 3306]**
    
    **Example — A mixed data type list:**  
    **mixed = ["nginx", 1.5, True, 443]**
    
    ---
    
    ### 🛠️ Key Characteristics of Lists
    
    - **Ordered:** Items maintain the order in which you define them.
    - **Indexed:** Each item has a position. The first item is at index **0**, second at **1**, and so on.
    - **Flexible:** You can store any data type — strings, integers, floats, booleans, even other lists.
    - **Dynamic:** Lists can grow or shrink as needed. You are not limited to a fixed size.
    
    ---
    
    ### 🕵️ Accessing Items in a List
    
    - Use the list name followed by the index number inside square brackets to access a specific item.
    - Indexing starts at **0**.
    
    **Example — Accessing the first server:**  
    **servers = ["web01", "db01", "cache01"]**  
    **first_server = servers[0]** → This gives you **"web01"**
    
    **Example — Accessing the last item using negative indexing:**  
    **last_server = servers[-1]** → This gives you **"cache01"**
    
    **Example — Accessing the second item:**  
    **second_server = servers[1]** → This gives you **"db01"**
    
    ---
    
    ### 📋 Comparison: List vs Other Simple Data Types
    
    | Feature | List | String | Integer |
    |---------|------|--------|---------|
    | Holds multiple items | ✅ Yes | ❌ No (single text) | ❌ No (single number) |
    | Mutable (changeable) | ✅ Yes | ❌ No (immutable) | ❌ No (immutable) |
    | Ordered | ✅ Yes | ✅ Yes | N/A |
    | Indexed access | ✅ Yes | ✅ Yes | N/A |
    | Mixed data types | ✅ Yes | ❌ No | ❌ No |
    
    ---
    
    ### 🧪 Practical Examples for Engineers
    
    **Example 1 — Storing environment names:**  
    **environments = ["dev", "staging", "prod"]**  
    **current_env = environments[1]** → Result: **"staging"**
    
    **Example 2 — Storing IP addresses as strings:**  
    **ip_list = ["10.0.1.1", "10.0.1.2", "10.0.1.3"]**  
    **first_ip = ip_list[0]** → Result: **"10.0.1.1"**
    
    **Example 3 — Storing configuration flags:**  
    **config_flags = [True, False, True, False]**  
    **first_flag = config_flags[0]** → Result: **True**
    
    **Example 4 — A list of lists (nested list):**  
    **network_config = [["web", "10.0.1.1"], ["db", "10.0.1.2"]]**  
    **first_pair = network_config[0]** → Result: **["web", "10.0.1.1"]**  
    **first_service = network_config[0][0]** → Result: **"web"**
    
    ---
    
    ### ✅ Summary
    
    - Lists are defined using **square brackets []**.
    - Items are separated by **commas**.
    - Lists are **ordered**, **mutable**, and can hold **mixed data types**.
    - Access items using **index numbers** starting from **0**.
    - Negative indexing (e.g., **-1**) lets you access items from the end of the list.
    
    Lists are a foundational building block in Python. Once you master defining and accessing them with square brackets, you'll be ready to explore more powerful list operations like adding, removing, and iterating over items.

=== "💻 Code Only"
    A list in Python is an ordered collection of items created by placing values inside square brackets `[]`, separated by commas.
    
    ---
    
    ### 🟢 Example 1: Creating an empty list
    
    This example shows how to define a list with no items inside it.
    
    ```python
    empty_list = []
    ```
    
    📤 Output: **[]**
    
    ---
    
    ### 🟢 Example 2: Creating a list of numbers
    
    This example shows a list containing three integer values.
    
    ```python
    numbers = [10, 20, 30]
    ```
    
    📤 Output: **[10, 20, 30]**
    
    ---
    
    ### 🟢 Example 3: Creating a list of strings
    
    This example shows a list containing text values.
    
    ```python
    cities = ["New York", "London", "Tokyo"]
    ```
    
    📤 Output: **['New York', 'London', 'Tokyo']**
    
    ---
    
    ### 🟢 Example 4: Creating a mixed-type list
    
    This example shows a list containing different data types together.
    
    ```python
    mixed = [42, "engineer", 3.14, True]
    ```
    
    📤 Output: **[42, 'engineer', 3.14, True]**
    
    ---
    
    ### 🟢 Example 5: Creating a list from variables
    
    This example shows how to use existing variables to build a list.
    
    ```python
    sensor_id = "TEMP-001"
    reading = 23.5
    unit = "Celsius"
    
    log_entry = [sensor_id, reading, unit]
    ```
    
    📤 Output: **['TEMP-001', 23.5, 'Celsius']**
    
    ---
    
    ### 📊 Quick Reference: Defining Lists
    
    | Syntax | Description | Example |
    |--------|-------------|---------|
    | `[]` | Empty list | `data = []` |
    | `[value1, value2, ...]` | List with items | `ports = [80, 443, 22]` |
    | `[var1, var2, ...]` | List from variables | `config = [host, port, timeout]` |
